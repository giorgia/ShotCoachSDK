import XCTest
@testable import ShotCoachCore

final class RuleTests: XCTestCase {
    // Each SCFrameRule implementation gets its own test method here.
    // Required: unit test + performance assertion (<80ms) per rule.
}
