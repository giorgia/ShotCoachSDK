import Foundation

/// Stores and retrieves the developer's cloud API key from the system Keychain.
/// Keys are never logged, printed, or embedded in URLs.
public struct SCKeychainService {
    public init() {}
}
