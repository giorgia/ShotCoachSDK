import SwiftUI

/// Onboarding screen where the user enters their OpenAI API key.
/// Key is stored via SCKeychainService — never written to UserDefaults or logs.
struct APIKeySetupView: View {
    var body: some View { EmptyView() }
}
