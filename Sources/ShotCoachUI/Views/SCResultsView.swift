import SwiftUI
import ShotCoachCore

/// Displays the SCCloudResult score, issues, and recommendations after photo capture.
public struct SCResultsView: View {
    public init() {}
    public var body: some View { EmptyView() }
}
