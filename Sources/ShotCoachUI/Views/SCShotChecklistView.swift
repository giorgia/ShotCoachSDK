import SwiftUI
import ShotCoachCore

/// Shows the session's required shots checklist and completion status.
public struct SCShotChecklistView: View {
    public init() {}
    public var body: some View { EmptyView() }
}
