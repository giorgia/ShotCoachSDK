import SwiftUI
import ShotCoachCore

/// Main entry-point view. Renders the camera preview with real-time guidance overlay.
/// Apply `.theme()` and `.onResult()` modifiers to configure.
public struct SCCameraGuidanceView: View {
    public init() {}
    public var body: some View { EmptyView() }
}
