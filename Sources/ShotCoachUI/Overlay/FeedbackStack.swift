import SwiftUI

/// Stacks multiple FeedbackPill views based on active SCFrameResult rule messages.
public struct FeedbackStack: View {
    public init() {}
    public var body: some View { EmptyView() }
}
