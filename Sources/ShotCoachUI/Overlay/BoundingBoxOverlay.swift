import SwiftUI

/// Draws bounding boxes over detected objects or regions of interest in the frame.
public struct BoundingBoxOverlay: View {
    public init() {}
    public var body: some View { EmptyView() }
}
